/assets/* https://assets.netlify.rodeo/:splat 301!
# The below rules provide a workaround to the "max hop" limit, when max_proxy_passes > 1
/test/* https://redirect-proxy-hop--netlifyrodeo.netlify.app/test-gb/:splat 200
/test-gb/* /index.html 200
# The below rules show how, as a rewrite (internal proxy), the functionality doesn't work by default
/demo /gb/demo.html 200
/* /index.html 200
